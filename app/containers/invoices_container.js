import { connect } from 'react-redux';

import InvoicesScreen from '../components/invoices_screen';

const mapStateToProps = (state) => {
  return {
  };
};

const mapDispatchToProps = (dispatch) => ({
});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(InvoicesScreen);
